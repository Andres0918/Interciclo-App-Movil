spring:
  application:
    name: app-module

server:
  port: 8082

eureka:
  client:
    service-url:
      defaultZone: http://eurekaserver:8761/eureka/  # ← Cambiar
    register-with-eureka: true
    fetch-registry: true
  instance:
    prefer-ip-address: true
    instance-id: ${spring.application.name}:${random.value}